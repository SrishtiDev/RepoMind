{
  "name": "repomind-mcp",
  "version": "1.0.0",
  "description": "MCP server for RepoMind — expose ingest & query tools to AI agents via stdio transport.",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "bin": {
    "repomind-mcp": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.15.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
